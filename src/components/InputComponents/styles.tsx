import { COLORS_DARK_THEME } from "theme/colors";

export const styles = {
    input: {
        display: 'flex',
        width: '100%',
        backgroundColor: COLORS_DARK_THEME.LIGHT_BACKGROUND,
        fontSize: '90%',
        padding: '0px 20px',
        borderRadius: '5px',
        alignItems: 'center'
    }
} as const
